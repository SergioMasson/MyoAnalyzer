<Window x:Class="MyoAnalyzer.XAML_blocks.ErroMassege"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyoAnalyzer"
        mc:Ignorable="d"
        Title="Error!" SizeToContent="Width" Height="180" Loaded="On_Loaded" ResizeMode="NoResize">
    <Grid>
        <Grid Margin="10" Height="Auto" Width="Auto">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" x:Name="ErroText"  Margin="10,10,10,30"/>
            <Button Grid.Row="1" x:Name="OkButton" Content="OK" HorizontalAlignment="Center" Margin="0,10,0,0" VerticalAlignment="Top" Width="90" Click="OK_Click" Height="30" FontWeight="Bold"/>
        </Grid>
    </Grid>
</Window>
